// utilizamos o loop em arrays para capturar os dados e manipularmos em nossas aplicações

const usuarios = ['Pedro', 'Lucas', 'Ribeiro']

for(let i= 0; i < usuarios.length; i++){
    console.log(`Listando os usuários: ${usuarios[i]}`); 
    // acima estamos acessando a lista de usuarios e acessando a posição deles através do indice que é o " i "
}